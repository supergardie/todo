<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/style.css" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
    <title><%= pagetitle %></title>
</head>

<body class="<%= title %>">    
        
    <div class="container">

        <div class="header">
            <div class="cell"><h1>To Do</h1></div>
            <div class="cell"><i class="material-icons">done</i></div>
            <div class="cell"><i class="material-icons">clear</i></div>
        </div>

        <form action="/todo" method="post">
            <input type="text" name="todoItem" />
            <button type="submit" name="add">ADD</button>
        </form>

        <form action="/edit" method="post">

            <% if(items.length) { %>
            <div class="todos">

                <% for(var ii = 0; ii < items.length; ii++) { %>
                <div class="row">
                    <div class="cell">
                        <% if(done[ii]) { %><s><% } %><%= items[ii] %><% if(done[ii]) { %></s><% } %>
                    </div>
                    <div class="cell">
                        <input type="checkbox" name="done" value="<%= num[ii] %>" />
                    </div>
                    <div class="cell">
                        <input type="checkbox" name="del" value="<%= num[ii] %>" />
                    </div>
                </div>
                <% } %>

            </div>
            <% } %>
            
            <div class="bottom">
                <button type="submit" name="del">UPDATE</button>
            </div>
        </form>
    </div>
</body>

</html>